[project]
name = "sip-proxy-panel-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.115.*",
  "uvicorn[standard]==0.32.*",
  "pyjwt==2.9.*",
  "bcrypt==4.2.*",
  "sqlalchemy==2.0.*",
  "pymysql==1.1.*",
  "psycopg[binary]==3.2.*",
  "docker==7.1.*",
  "python-multipart==0.0.20",
  "pydantic-settings==2.14.*",
]

[project.optional-dependencies]
test = [
  "pytest==8.3.*",
  "httpx==0.27.*",
  "testcontainers[mariadb,postgres]==4.8.*",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
