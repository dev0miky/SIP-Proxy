[project]
name = "sip-proxy-panel-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.136.*",
  "uvicorn[standard]==0.47.*",
  "pyjwt==2.12.*",
  "bcrypt==4.2.*",
  "sqlalchemy==2.0.*",
  "pymysql==1.1.*",
  "psycopg[binary]==3.2.*",
  "docker==7.1.*",
  "python-multipart==0.0.20",
  "pydantic-settings==2.14.*",
]

[project.optional-dependencies]
test = [
  "pytest==8.3.*",
  "httpx==0.28.*",
  "testcontainers[mariadb,postgres]==4.14.*",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
